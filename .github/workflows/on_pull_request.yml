name: Tests
'on':
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  test:
    name: Node.js
    runs-on: puppeteer:latest
    steps:
      - uses: actions/checkout@v2
      - name: Run All Node.js Tests in Docker
        run: yarn \
          yarn samples